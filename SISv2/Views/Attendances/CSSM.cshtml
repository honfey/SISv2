@model IEnumerable<SISv2.Models.ClassStudent>

@{
    ViewBag.Title = "Check Specific Student Module";
}

<h2>Check Specific Student Module</h2>

<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Course_Module.Module.ModuleCode)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>            
            @Html.ActionLink(item.Course_Module.Module.ModuleCode, "CheckIndividualAttendance", new { id = item.Id })
        </td>
        @*<td>
            @Html.DisplayFor(modelItem => item.Student.Name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>*@
    </tr>
}

</table>
